<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pacman">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="Activities.PauseActivity"></activity>
        <activity android:name="Activities.LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Activities.MainActivity" />
        <activity android:name="Activities.CreatorsActivity" />
        <activity android:name="Activities.ScoreActivity" />
        <activity android:name="Activities.PlayActivity" />
        <service android:name="Activities.BackgroundMusicService"
            android:enabled="true"/>
    </application>

</manifest>